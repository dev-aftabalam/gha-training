name: 01 Understanding Contexts
on: push
jobs: 
    event-info-job:
        runs-on: ubuntu-latest
        steps:
            -name: "trigger event"
             run: echo "My trigger event is ${{ github.event_name }}"